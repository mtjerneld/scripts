# Azure Security Audit Tool - Environment Variables
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# Azure Authentication (for Connect-AuditEnvironment)
# Entra ID (formerly Azure AD) Tenant ID - find it in Azure Portal > Entra ID > Overview
AZURE_TENANT_ID=your-tenant-id-here

# Service Principal Application (Client) ID
AZURE_CLIENT_ID=your-client-id-here

# Service Principal Client Secret
AZURE_CLIENT_SECRET=your-client-secret-here

# OpenAI Configuration (for AI-powered analysis)
# Required: Your OpenAI API key (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: OpenAI Model to use (default: gpt-4o-mini)
# Options: gpt-4o-mini, gpt-4o, gpt-4-turbo-preview, gpt-5-mini, gpt-5, etc.
OPENAI_MODEL=gpt-5-nano

# Optional: Cost Control Settings
# Maximum cost per analysis run in USD (default: 0.10)
OPENAI_MAX_COST_USD_PER_RUN=0.10

# Optional: Token Pricing (for cost estimation, per 1M tokens - matches OpenAI pricing format)
# Pricing varies by model. Examples:
#   gpt-5-nano:     Input $0.05,  Output $0.40
#   gpt-5-mini:     Input $0.25,  Output $2.00
#   gpt-4o-mini:    Input ~$0.15, Output ~$0.60
#   gpt-4.1-mini:   Input $0.40,  Output $1.60
# Check https://openai.com/api/pricing/ for current pricing
OPENAI_PRICE_INPUT_PER_1M_USD=0.05

# Output token price per 1M tokens in USD
OPENAI_PRICE_OUTPUT_PER_1M_USD=0.40

# Optional: Advanced Settings
# Request timeout in seconds (default: 60)
OPENAI_TIMEOUT_SECONDS=60

# Reasoning effort for gpt-5 models: low, medium, or high (default: low)
# Only applies to gpt-5 series models (gpt-5-mini, gpt-5, etc.)
OPENAI_REASONING_EFFORT=low

# Optional: Max output tokens for OpenAI API (default: 8000)
# Useful for reasoning models (gpt-5 series) that need more tokens: reasoning tokens + output tokens
OPENAI_MAX_OUTPUT_TOKENS=16000

